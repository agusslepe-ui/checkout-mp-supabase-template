# Cierre T-021 / DEC-026 — 2026-09-16

## Estado final

- **T-021 — Selección real de sucursal MiCorreo: COMPLETADA.**
- **DEC-026 — Selección autoritativa de agencia: ACEPTADA.**
- Auditoría previa: **APROBADA CON OBSERVACIONES**, sin bloqueadores de implementación.
- Suite local previa al cutover: **276/276 tests**, 4 suites, sin red real.
- Migración `006_add_order_shipping_agency.sql`: **APLICADA EN PRODUCCIÓN**.
- Runtime T-021: **DESPLEGADO EN EASYPANEL** después de aplicar 006.

## Cutover productivo verificado

La secuencia aplicada fue:

1. Precheck de producción: RPC T-020 activa con **20 parámetros**.
2. Precheck: las seis columnas `shipping_agency_*` todavía no existían.
3. Aplicación manual de `supabase/migrations/006_add_order_shipping_agency.sql` en Supabase.
4. Deploy manual inmediato de T-021 en EasyPanel.
5. Postcheck: `create_pending_order_with_items` activa con **26 parámetros**.
6. Postcheck: existen las seis columnas de agencia, todas `text` y nullable.
7. Postcheck de permisos: solo `postgres` y `service_role` poseen `EXECUTE`; no aparecen `anon`, `authenticated` ni `PUBLIC`.

No quedó una migración parcial durante el primer intento fallido: el error ocurrió dentro de la transacción y la RPC continuó mostrando 20 parámetros hasta ejecutar correctamente el archivo 006 exacto.

## QA real — HOME

Se validó el flujo productivo hasta Mercado Pago sin completar el pago.

Orden creada:

- `id = 59`
- `status = pending`
- `products_subtotal = 2000.00`
- `shipping_amount = 8046.00`
- `amount = 10046`
- `currency = ARS`
- `shipping_provider = micorreo`
- `shipping_option_id = micorreo:home:classic`
- `shipping_delivery_type = home`
- `shipping_service = classic`

Resultado esperado confirmado:

- `2000 + 8046 = 10046`.
- Los seis campos `shipping_agency_*` quedaron `NULL`.
- HOME no requiere snapshot de agencia.

## QA real — AGENCY

Se validó por primera vez en producción el flujo real de `GET /agencies` de MiCorreo desde el backend, la selección de sucursal en frontend, la revalidación autoritativa en checkout y la persistencia del snapshot antes de redirigir a Mercado Pago.

Orden creada:

- `id = 60`
- `status = pending`
- `products_subtotal = 2000.00`
- `shipping_amount = 5428.00`
- `amount = 7428`
- `currency = ARS`
- `shipping_provider = micorreo`
- `shipping_option_id = micorreo:agency:classic`
- `shipping_delivery_type = agency`
- `shipping_service = classic`
- `shipping_agency_code = J0000`
- `shipping_agency_name = SAN JUAN`
- `shipping_agency_street_name = AV JOSE I DE LA ROZA`
- `shipping_agency_street_number = 223`
- `shipping_agency_locality = SAN JUAN`
- `shipping_agency_postal_code = J5402DBC`

Resultado esperado confirmado:

- `2000 + 5428 = 7428`.
- El browser envió solo la identidad de sucursal; el snapshot persistido provino de la revalidación backend contra MiCorreo.
- La identidad autoritativa de sucursal queda representada por `shipping_agency_code`.

## Seguridad validada

La RPC productiva `public.create_pending_order_with_items` quedó ejecutable únicamente por:

- `postgres`
- `service_role`

No se otorgó `EXECUTE` a `anon`, `authenticated` ni `PUBLIC`.

El navegador no es autoridad sobre precio de envío, total, provider ni snapshot de sucursal. El backend recotiza y revalida antes de persistir.

## Alcance que NO forma parte de este cierre

- No se completó un pago real durante este QA; las órdenes 59 y 60 quedaron correctamente en `pending`.
- El **paid QA de T-020** continúa como tarea separada.
- **DEC-025** no se acepta automáticamente por cerrar T-021.
- No se llamó ni implementó `POST /shipping/import`.
- No se creó un envío físico ni se obtuvo tracking/etiqueta.
- T-017 / DEC-022 de idempotencia durable continúan pendientes.
- No existe stock real por SKU; `maxQuantity: 4` sigue siendo un límite temporal, no inventario.

## Bloqueadores antes del lanzamiento comercial

- Reemplazar perfiles TEMPORAL/QA de `300 g / 5 × 25 × 35 cm` por peso y dimensiones reales del paquete para 1–4 unidades.
- Repetir cotizaciones reales después de cargar perfiles definitivos.
- Restaurar el precio comercial definitivo; ARS 1.000 sigue siendo precio de prueba.
- Rotar credenciales privadas previamente expuestas antes de declarar producción pública.
- Resolver stock real y la deuda de idempotencia durable según el alcance que se apruebe.
- Auditar las vulnerabilidades npm registradas en tareas anteriores.

## Resultado

T-021 queda **COMPLETADA** y DEC-026 queda **ACEPTADA** con evidencia real de producción para HOME y AGENCY hasta la creación del pedido `pending` y la redirección a Mercado Pago.

La siguiente evolución logística natural es la Etapa D: creación del envío post-pago mediante `/shipping/import`, manteniéndola separada de este cierre.